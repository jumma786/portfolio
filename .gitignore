# OS / editor cruft
.DS_Store
Thumbs.db
Desktop.ini
*.swp
.idea/
.vscode/

# Original certificates / identity documents stay local — never publish.
# They carry registration and roll numbers, dates of birth, parents' names
# and photographs. Only the redacted copies in assets/certificates/ — which
# have those fields removed from the file, not merely covered — are served.
/*.pdf

# Claude Code: the dev-server config is shared, local MCP/tool prefs are not.
.claude/settings.local.json
